// Reconstructed from MikeCore — 5 properties

struct MDToolbarPositionCtrl {
    bool                     _isMuted;
    bool                     _isSolo;
    float                    _volume;
    float                    _amplitudeScaleFactor;
    bool                     _isTrackTitleEditable;
};